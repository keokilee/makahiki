# Nose tests.
LOCAL_INSTALLED_APPS = (
  # Testing
	'django_nose',
)

# Hudson/Jenkins use the folder name "workspace"
ROOT_URLCONF = 'workspace.urls'

# Use Nose as the test runner.
TEST_RUNNER = 'django_nose.NoseTestSuiteRunner'

SOUTH_TESTS_MIGRATE = False